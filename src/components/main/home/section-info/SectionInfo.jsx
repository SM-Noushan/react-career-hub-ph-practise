const SectionInfo = ({ title, desc }) => {
    return (
        <div className="text-center">
            <h1>{title}</h1>
            <p>{desc}</p>
        </div>
    );
};

export default SectionInfo;